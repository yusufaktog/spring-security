version: '3.8'
services:
  db:
    image: mysql
    restart: always
    container_name: jwt_test
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: jwt_test
      MYSQL_USER: user
      MYSQL_PASSWORD: password
    ports:
      - '3306:3306'